require "rails_helper"

describe "artist index page" do
  it "has a picture for every artist"
  it "has a link to every artists show page"
  it "#optional: artists can be sorted by name(ascend/descend) and creation date(ascend/descend)"
end
